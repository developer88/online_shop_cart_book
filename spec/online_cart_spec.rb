require 'spec_helper'

describe OnlineCart do
  it 'has a version number' do
    expect(OnlineCart::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
