require 'spec_helper'

describe SuperOnlineCart do
  it 'has a version number' do
    expect(SuperOnlineCart::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
